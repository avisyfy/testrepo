pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo "Welcome to jenkins"
                sh "chmod 777 file1"
                echo "changed permissions"
                 sh "file1"
                
            }
        }
    }
}
